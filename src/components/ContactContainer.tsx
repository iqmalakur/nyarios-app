import { Component, ReactNode } from "react";

class ContactContainer extends Component {
  render(): ReactNode {
    return (
      <div id="contact-container" className="h-full w-10 bg-cyan-950"></div>
    );
  }
}

export default ContactContainer;
