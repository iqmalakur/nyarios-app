import { Component, ReactNode } from "react";

class ChatContainer extends Component {
  render(): ReactNode {
    return <div id="chat-container" className="h-full"></div>;
  }
}

export default ChatContainer;
